#include <vector>
#include <iostream>
#include <cstddef>

int main()
{
    const std::size_t N = 1000;

    // Generate two random vectors here
    std::vector<float> x(N);
    std::vector<float> y(N);

    // calculate their sum
}